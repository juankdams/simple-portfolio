---
// Botón flotante para volver al top
---
<button
  id="back-to-top"
  class="fixed bottom-6 right-6 z-50 bg-primary-light dark:bg-primary-dark text-white rounded-full p-3 shadow-lg hover:bg-primary-dark dark:hover:bg-primary-light hover:text-primary-light dark:hover:text-primary-dark transition-all duration-300"
  aria-label="Volver arriba"
  style="display:none;"
>
  ↑
</button>

<script>
  // Mostrar el botón cuando se hace scroll
  window.addEventListener('scroll', () => {
    const btn = document.getElementById('back-to-top');
    if (!btn) return;
    if (window.scrollY > 200) {
      btn.style.display = 'block';
    } else {
      btn.style.display = 'none';
    }
  });
  // Scroll suave al top
  document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('back-to-top');
    if (!btn) return;
    btn.onclick = () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    };
  });
</script>
